**Coding Tools MCP Desktop 0.3.18 / Core 0.3.7 — macOS release verification**

Release date: 2026-09-09. Target: macOS arm64.

This release exposes the lightweight external runtime model directly in the desktop UI and native menu-bar Resources menu. MCP Runtime and Playwright readiness/version are visible, the runtime can be prepared or rebuilt explicitly, uv/cloudflared repair is labeled separately, Chrome/CDP/Chrome Bridge states are shown independently, and macOS Accessibility and Screen Recording permission state/actions are exposed through the signed App Helper.

The release bundle remains lightweight. The application contains the pure-Python Core 0.3.7 wheel, hashed production requirements, icon resources, and the signed native App Helper. Final package auditing found no bundled Python interpreter, Playwright Node executable, Chromium, or Chrome binary.

Developer ID signing identity: `Developer ID Application: Yongjian Li (2NLAH5MYH8)`. The helper was signed first with identifier `com.codingtoolsmcp.app-helper`, followed by the containing application with Hardened Runtime and secure timestamps. The containing app identifier is `com.codingtoolsmcp.desktop`.

Apple notarization:

- App ZIP submission `5260bd4d-47f3-4996-ae27-38ad2d1a5b31`: **Accepted**. The ticket was stapled to and validated on the application before DMG creation.
- Final drag-install DMG submission `9219aada-6b4f-435d-8838-432856e54589`: **Accepted**. The DMG ticket was stapled and validated.

The final DMG uses a standard Finder drag-install layout: the Coding Tools MCP application icon is rendered at 112 px on the left, an `/Applications` shortcut is shown on the right, and the persisted positions read back as app `{165,185}` and Applications `{435,185}` after remounting the final image.

Final mounted-image verification passed `codesign --verify --deep --strict`, helper signature verification, `spctl --assess --type execute` with source `Notarized Developer ID`, app and DMG stapler validation, DMG codesign verification, Finder layout readback, and `hdiutil verify`. The mounted application reports Desktop `0.3.18`; its runtime manifest reports Core `0.3.7` with environment key `0.3.7-cb5292d05f7d7075119a`.

Final signed, notarized and stapled DMG size is 4,289,937 bytes (about 4.09 MiB). The signed native helper is 147,584 bytes and the bundled Core wheel is 141,219 bytes.

DMG SHA-256: `d194f6ab3900fce845e8aefb21ecc57c15a220e5112e7e22636d556a4b581438`.

Final local artifact: `apps/desktop-client/src-tauri/target/release/bundle/dmg/Coding Tools MCP_0.3.18_aarch64.dmg`.
