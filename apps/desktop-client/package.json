{
  "name": "coding-tools-mcp-desktop",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:runtime": "node ./build-bundled-runtime.mjs",
    "build:bundle": "npm run build:runtime && npm run build",
    "build:dev-app": "CODING_TOOLS_MCP_BUILD_CHANNEL=preview npm run tauri -- build --config tauri.dev.conf.json --bundles app --no-sign",
    "build:dmg": "bash ./create-dmg.sh",
    "test": "vitest run",
    "check": "tsc --noEmit && vitest run",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-clipboard-manager": "^2.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.7.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  },
  "releasePlatforms": [
    "macos"
  ]
}
